---
id: "03-requirements-engineer"
number: 3
cluster: "product"
accent: "red"
phase: "Discovery"
title: "Engenheiro de Requisitos"
oneLiner: 'Codifica requisitos em EARS.'
order: 3
updated: "2026-04-24"
---

> The Requirements Engineer é a persona que encodes, decomposes, and traces requirements with discipline. Em um SDLC AI-native, o Requirements Engineer opera uma stack de primitivas validadas that convert draft intent into auditable EARS clauses.

## Resumo executivo

The Requirements Engineer decomposes approved specifications into atomic, testable EARS requirements and maintains the traceability matrix from requirement to test to deployed artifact. Em um SDLC AI-native, o Requirements Engineer opera dentro da fase de Discovery phase com um conjunto fixo de primitivas: um agente EARS encoding, quatro slash prompts, instruções escopadas, hooks validados por schema, and uma lista curada de MCPs validados. Saídas principais são atomic requirement files, gap analyses, traceability reports, and requirement review records.

## Papel e responsabilidades

Pense no Requirements Engineer like a legal codifier who turns a legislator's intent into numbered statutes that courts can apply without interpretation. The legislator proposes, the codifier transforms, and every future judgment cites a clause by number. Em um SDLC AI-native, o Product Owner proposes in `SPECIFICATION.md`, the Requirements Engineer codifies in atomic EARS form, and every test, commit, and release note cites a requirement ID.

Responsabilidades principais:

- Decompose every approved spec into atomic EARS requirements under `docs/requirements/`
- Maintain the traceability matrix that links each requirement to its acceptance criteria, tests, pull requests, and deployed artifacts
- Run gap scans to detect requirements without tests, tests without requirements, and deployments without either
- Run requirement reviews with the Product Owner, Software Architect, and QA Engineer at defined gates
- Operate the EARS Encoder agent and the `/ears`, `/traceability`, `/gap-scan`, `/requirement-review` prompts
- Govern the requirement change log, ensuring every change references an approved spec diff
- Publish the readiness report that releases depend on

## Jobs to be done

1. Como Requirements Engineer, eu quero decompose an approved spec into atomic requirements within hours, para que development is never blocked on clarification.
2. Como Requirements Engineer, eu quero every requirement in strict EARS form, para que agents and humans parse it identically.
3. Como Requirements Engineer, eu quero the traceability matrix to regenerate on every merge, para que audit answers are always current.
4. Como Requirements Engineer, eu quero gap scans to run on every pull request, para que tests and requirements never diverge silently.
5. Como Requirements Engineer, eu quero requirement reviews to be structured and recorded, para que decisions persist beyond the meeting.
6. Como Requirements Engineer, eu quero the readiness report to refuse release if any gap is unresolved, para que scope discipline is enforced at the gate.

## Dores antes da era AI-native

1. **Specs too coarse to test**. High-level specs cannot be bound to test cases. Developers improvise decomposition inconsistently.
2. **Traceability built once and abandoned**. Matrix spreadsheets go stale within a sprint. Audit preparation becomes a rebuild.
3. **Gaps discovered at release**. Missing tests and orphan features surface at the release gate, forcing schedule slips.
4. **Reviews without records**. Clarifications happen verbally. Next sprint, the same question is asked again.
5. **Requirement churn untracked**. Changes are made in place without a diff history. Regressions in scope are invisible.

## Fluxo diário AI-native

The Requirements Engineer operates a fixed loop each day. O loop usa primitivas do GitHub Copilot dentro do Visual Studio Code e Claude Code no terminal, além de um pequeno catálogo de MCPs validados para contexto externo.

### Setup da manhã

1. Abrir o repositório no Visual Studio Code. GitHub Copilot Chat carrega `AGENTS.md` and the scoped requirement instructions.
2. Fazer pull da última `main` e revisar mudanças noturnas spec changes by the Product Owner.
3. Run `/gap-scan` to produce the current gap report: requirements without tests, tests without requirements, deployments without either.
4. Review the traceability dashboard generated from the Azure MCP Server and GitHub MCP telemetry.

### Execução no meio do dia

1. **Decomposition**. Invoke `/ears` on each new spec section. The EARS Encoder agent produces atomic requirements with unique IDs, each in strict `WHEN ... THE system SHALL ...` form.
2. **Traceability**. Invoke `/traceability` to bind each new requirement to existing tests, pull requests, and deployment records. The agent updates the matrix and flags orphans.
3. **Gap closure**. For each gap flagged in the morning scan, either author the missing requirement, raise a test task for QA Engineer, or file an ADR if architectural change is required.
4. **Cross-persona sync**. Use the Microsoft 365 Agents SDK MCP to raise clarification requests in the Teams channel when spec intent is ambiguous.

### Revisão no fim da tarde

1. Invoke `/requirement-review` to run the structured review session with the Product Owner, Software Architect, and QA Engineer. The agent prepares the agenda from the day's diffs and records decisions against requirement IDs.
2. Abrir um pull request on the requirement files. GitHub Copilot Code Review comments on EARS compliance; human reviewers approve content.
3. Update the readiness report. A post-merge hook regenerates the report with gap counts and blocker lists.
4. Publish the daily requirement digest to the product Teams channel via the Microsoft 365 Agents SDK.

## Primitivas recomendadas

### Agente

| Agente | Arquivo | Propósito |
|-------|------|---------|
| `ears-encoder` | `.github/agents/ears-encoder.agent.md` | Decompose specs into atomic EARS requirements, maintain traceability, run gap scans |

The EARS Encoder uses `claude-sonnet-4-6` by default. Tools: `read`, `edit`, `search`, `grep`, `glob`. No `bash` access. Extended thinking is enabled for `/gap-scan` only, where cross-document correlation benefits from deep reasoning.

### Slash prompts

| Comando | Arquivo | Propósito |
|---------|------|---------|
| `/ears` | `.github/prompts/ears.prompt.md` | Decompose a spec section into atomic EARS requirements |
| `/traceability` | `.github/prompts/traceability.prompt.md` | Bind requirements to tests, PRs, and deployments, regenerate the matrix |
| `/gap-scan` | `.github/prompts/gap-scan.prompt.md` | Detect requirements without tests, tests without requirements, orphan deployments |
| `/requirement-review` | `.github/prompts/requirement-review.prompt.md` | Prepare and record the structured review session |

### Instruções escopadas

O escopo `applyTo` reduz o custo de tokens em aproximadamente 68 por cento comparado com instruções globais.

| Escopo (`applyTo`) | Arquivo | Propósito |
|-------------------|------|---------|
| `docs/requirements/**/*.md` | `.github/instructions/requirements.instructions.md` | Atomic EARS format, ID schema, cross-reference rules |
| `docs/traceability/**/*.md` | `.github/instructions/traceability.instructions.md` | Matrix schema, regeneration rules, audit evidence format |
| `docs/reviews/**/*.md` | `.github/instructions/reviews.instructions.md` | Review agenda template, decision record format |

### Hooks

Hooks custam zero tokens de LLM. Eles são a camada de governança mais forte for requirements.

- `pre-commit`: reject any requirement file with non-atomic or non-EARS clauses, or with duplicate IDs
- `post-commit`: regenerate the traceability matrix and the readiness report
- `pre-merge`: block merge of any requirement change that introduces a new gap without a linked follow-up issue

## MCPs validados

| MCP | Propósito | Dono |
|-----|---------|-------|
| [GitHub MCP Server](https://github.com/github/github-mcp-server) | Ler pull requests, tests, and Actions runs to populate the traceability matrix | GitHub (official) |
| [Azure DevOps MCP Server](https://github.com/microsoft/azure-devops-mcp) | Ler work items do Azure Boards and test plans when the team uses Azure DevOps | Microsoft (official) |
| [Azure MCP Server](https://github.com/Azure/azure-mcp) | Consultar Application Insights to confirm deployed artifacts match requirement IDs | Microsoft (official) |
| [Microsoft Learn Docs MCP](https://github.com/microsoftdocs/mcp) | Basear-se na documentação atual de produtos Microsoft | Microsoft (official) |
| [Microsoft 365 Agents SDK MCP](https://learn.microsoft.com/microsoft-365/agents-sdk/) | Levantar threads de clarificação no Teams and registrar decisões do Outlook | Microsoft (official) |

## Exemplos reais

### Example 1: decompose an approved spec section

**Entrada**: A spec section on contract renewal covering initiation, eligibility, and notification, approved by the Product Owner.

**Invocação**: `/ears` followed by `/traceability`.

**Saída esperada**:

1. A `docs/requirements/contract-renewal/` directory with twelve atomic requirement files, each with a unique ID and strict EARS clause.
2. A regenerated traceability matrix that binds three pre-existing tests to two of the new requirements and flags the remaining ten as gaps.
3. A pull request with Copilot Code Review comments on clause shape and ID consistency.

### Example 2: gap closure before release

**Entrada**: A release candidate with 146 merged requirements. The morning `/gap-scan` report flags six requirements without passing tests and two tests without requirements.

**Invocação**: `/gap-scan` followed by `/requirement-review`.

**Saída esperada**:

1. Six tasks in GitHub Projects created via the GitHub MCP, each linked to the requirement anchor and assigned to QA Engineer.
2. Two requirement files authored to bind the orphan tests to requirement IDs.
3. A recorded review session in `docs/reviews/2026-q3-release-readiness.md` with decisions tagged by requirement ID.
4. A readiness report update that reopens the release gate until all eight items close.

## Anti-padrões

1. **Compound requirements**. Clauses that bundle two or more conditions cannot be tested atomically. Mitigação: the `/ears` agent and the `pre-commit` hook reject non-atomic clauses.
2. **Manual traceability**. Spreadsheets cannot be trusted on a sprint boundary. Mitigação: the matrix regenerates on `post-commit`.
3. **Silent ID collisions**. Reused IDs break audit trails. Mitigação: the `pre-commit` hook rejects duplicates across the `docs/requirements/` tree.
4. **Reviews without records**. Verbal clarifications evaporate. Mitigação: `/requirement-review` enforces a recorded decision for every agenda item.
5. **Gaps waived verbally**. A waived gap without an ADR or follow-up issue re-emerges at release. Mitigação: `pre-merge` blocks the waiver path.

## KPIs e métricas de impacto

| KPI | Linha base | Meta | Medição |
|-----|----------|--------|-------------|
| Requirements in atomic EARS | 30 percent | 100 percent | Requirements linter in GitHub Actions |
| Traceability freshness, last regeneration | 7 days | < 1 hour | Post-commit hook timestamp |
| Gap count at release gate | 14 | 0 | Readiness report |
| Requirement review cycle time | 2 weeks | < 1 week | PR timestamps |
| Orphan test rate | 12 percent | < 2 percent | Gap scan output |
| Audit query response time | 1 day | < 1 hour | Traceability matrix query log |

## Maturidade em quatro níveis

| Nível | Nome | Marcadores |
|-------|------|---------|
| L1 | Manual | Prose specs, ad-hoc decomposition, traceability built by hand |
| L2 | Assistido | Copilot used to polish requirement phrasing, no atomic discipline |
| L3 | Aumentado | EARS Encoder agent, quatro slash prompts, instruções escopadas, matrix auto-generated, gap scans on PR |
| L4 | Autônomo | Full primitives kit, hooks enforced, readiness report live, reviews recorded automatically, audit response within the hour |

## Integração com outras personas

- **Do Product Owner**: approved `SPECIFICATION.md` with numbered EARS requirements as the canonical input
- **Do Business Manager**: KPI-bound outcomes that inform gap prioritization
- **Para o Software Architect**: stable requirement IDs that anchor `CODEMAP.md` and API contracts
- **Para o QA Engineer**: atomic requirements that drive test case generation and coverage reports
- **Para o Developer**: requirement anchor on every PR enabling implementation traceability
- **Para o Compliance Auditor**: regenerated matrix as audit evidence on demand
- **Para o Release Manager**: readiness report gating the release cut

## Glossário

- **EARS**: Easy Approach to Requirements Syntax. The canonical `WHEN ... THE system SHALL ...` form.
- **Atomic requirement**: a single testable clause with a unique ID, no conjunctions that introduce independent conditions.
- **Traceability matrix**: generated table binding each requirement to acceptance criteria, tests, pull requests, and deployments.
- **Gap scan**: automated sweep that detects requirements without tests, tests without requirements, and deployments without either.
- **Readiness report**: generated document summarizing release gate status against open requirement gaps.
- **Requirement review**: structured, recorded session that approves or defers requirement changes with named participants.

## Referências

- [GitHub Copilot documentation](https://docs.github.com/en/copilot) — modo agente, prompts e instruções
- [Azure Boards documentation](https://learn.microsoft.com/azure/devops/boards/) — rastreamento de work items for requirement-linked tasks
- [GitHub Actions documentation](https://docs.github.com/en/actions) — automação para traceability regeneration and gap scans
- [Application Insights documentation](https://learn.microsoft.com/azure/azure-monitor/app/app-insights-overview) — deployed artifact telemetry for requirement verification
- [Microsoft Learn, Well-Architected Framework](https://learn.microsoft.com/azure/well-architected/) — operational excellence and audit evidence guidance
